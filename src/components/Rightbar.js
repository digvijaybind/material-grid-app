import {Box} from "@mui/material";
import React from "react";

const Rightbar = () => {
  return (
    <Box bgcolor="blue" flex={2} p={2} sx={{display: {sm: "block"}}}>
      Rightbar
    </Box>
  );
};

export default Rightbar;
