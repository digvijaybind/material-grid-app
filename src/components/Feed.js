import {Box} from "@mui/material";
import React from "react";

const Feed = () => {
  return (
    <Box bgcolor="skyblue" flex={4} p={2} sx={{display: {sm: "block"}}}>
      Feed
    </Box>
  );
};
export default Feed;
